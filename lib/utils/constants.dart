import 'package:flutter/material.dart';

class AppColors {
  static const primary = Color(0xFF37474F);
  static const secondary = Color(0xFF607D8B);
  static const card = Color(0xFF455A64);
  static const fallingObject = Color(0xFF90A4AE);
  static const button = Color(0xFF78909C);
  static const background = Color(0xFFF5F5F5);
  static const text = Colors.white;
}

class AppIcons {
  static const iconList = [
    Icons.star,
    Icons.favorite,
    Icons.android,
    Icons.home,
    Icons.ac_unit,
    Icons.alarm,
    Icons.cake,
    Icons.flight,
  ];
}
